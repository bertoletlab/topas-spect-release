# Documentation build dependencies (Read the Docs / local `mkdocs serve`).
# Pinned to a known-good range: an unbounded mkdocs-material lets Read the Docs install a
# breaking new major on a fresh build while local builds stay on an older one.
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
